// Re-export types from the new data layer for backward compatibility
export type {
	User,
	CreateUserData,
	UpdateUserData,
	UserPayload,
	JWTPayload,
	RegisterPayload,
	LoginPayload
} from '../data/models/User.js'; 